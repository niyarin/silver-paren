(define-library (silver-paren features-libraries)
   (import (scheme base))
   (export silver-paren-features-srfi silver-paren-features-r7rs)
   (begin
     (define *srfi-libraries* '())
     (define *r7rs-libraries* '()))

   (cond-expand ((library (srfi 0)) (begin (set! *srfi-libraries* (cons '(srfi 0) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 1)) (begin (set! *srfi-libraries* (cons '(srfi 1) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 2)) (begin (set! *srfi-libraries* (cons '(srfi 2) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 4)) (begin (set! *srfi-libraries* (cons '(srfi 4) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 5)) (begin (set! *srfi-libraries* (cons '(srfi 5) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 6)) (begin (set! *srfi-libraries* (cons '(srfi 6) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 7)) (begin (set! *srfi-libraries* (cons '(srfi 7) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 8)) (begin (set! *srfi-libraries* (cons '(srfi 8) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 9)) (begin (set! *srfi-libraries* (cons '(srfi 9) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 10)) (begin (set! *srfi-libraries* (cons '(srfi 10) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 11)) (begin (set! *srfi-libraries* (cons '(srfi 11) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 13)) (begin (set! *srfi-libraries* (cons '(srfi 13) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 14)) (begin (set! *srfi-libraries* (cons '(srfi 14) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 16)) (begin (set! *srfi-libraries* (cons '(srfi 16) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 17)) (begin (set! *srfi-libraries* (cons '(srfi 17) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 18)) (begin (set! *srfi-libraries* (cons '(srfi 18) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 19)) (begin (set! *srfi-libraries* (cons '(srfi 19) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 21)) (begin (set! *srfi-libraries* (cons '(srfi 21) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 22)) (begin (set! *srfi-libraries* (cons '(srfi 22) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 23)) (begin (set! *srfi-libraries* (cons '(srfi 23) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 25)) (begin (set! *srfi-libraries* (cons '(srfi 25) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 26)) (begin (set! *srfi-libraries* (cons '(srfi 26) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 27)) (begin (set! *srfi-libraries* (cons '(srfi 27) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 28)) (begin (set! *srfi-libraries* (cons '(srfi 28) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 29)) (begin (set! *srfi-libraries* (cons '(srfi 29) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 30)) (begin (set! *srfi-libraries* (cons '(srfi 30) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 31)) (begin (set! *srfi-libraries* (cons '(srfi 31) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 34)) (begin (set! *srfi-libraries* (cons '(srfi 34) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 35)) (begin (set! *srfi-libraries* (cons '(srfi 35) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 36)) (begin (set! *srfi-libraries* (cons '(srfi 36) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 37)) (begin (set! *srfi-libraries* (cons '(srfi 37) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 38)) (begin (set! *srfi-libraries* (cons '(srfi 38) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 39)) (begin (set! *srfi-libraries* (cons '(srfi 39) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 41)) (begin (set! *srfi-libraries* (cons '(srfi 41) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 42)) (begin (set! *srfi-libraries* (cons '(srfi 42) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 43)) (begin (set! *srfi-libraries* (cons '(srfi 43) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 44)) (begin (set! *srfi-libraries* (cons '(srfi 44) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 45)) (begin (set! *srfi-libraries* (cons '(srfi 45) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 46)) (begin (set! *srfi-libraries* (cons '(srfi 46) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 47)) (begin (set! *srfi-libraries* (cons '(srfi 47) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 48)) (begin (set! *srfi-libraries* (cons '(srfi 48) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 49)) (begin (set! *srfi-libraries* (cons '(srfi 49) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 51)) (begin (set! *srfi-libraries* (cons '(srfi 51) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 54)) (begin (set! *srfi-libraries* (cons '(srfi 54) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 55)) (begin (set! *srfi-libraries* (cons '(srfi 55) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 57)) (begin (set! *srfi-libraries* (cons '(srfi 57) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 58)) (begin (set! *srfi-libraries* (cons '(srfi 58) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 59)) (begin (set! *srfi-libraries* (cons '(srfi 59) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 60)) (begin (set! *srfi-libraries* (cons '(srfi 60) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 61)) (begin (set! *srfi-libraries* (cons '(srfi 61) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 62)) (begin (set! *srfi-libraries* (cons '(srfi 62) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 63)) (begin (set! *srfi-libraries* (cons '(srfi 63) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 64)) (begin (set! *srfi-libraries* (cons '(srfi 64) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 66)) (begin (set! *srfi-libraries* (cons '(srfi 66) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 67)) (begin (set! *srfi-libraries* (cons '(srfi 67) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 69)) (begin (set! *srfi-libraries* (cons '(srfi 69) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 70)) (begin (set! *srfi-libraries* (cons '(srfi 70) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 71)) (begin (set! *srfi-libraries* (cons '(srfi 71) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 72)) (begin (set! *srfi-libraries* (cons '(srfi 72) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 74)) (begin (set! *srfi-libraries* (cons '(srfi 74) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 78)) (begin (set! *srfi-libraries* (cons '(srfi 78) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 86)) (begin (set! *srfi-libraries* (cons '(srfi 86) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 87)) (begin (set! *srfi-libraries* (cons '(srfi 87) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 88)) (begin (set! *srfi-libraries* (cons '(srfi 88) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 89)) (begin (set! *srfi-libraries* (cons '(srfi 89) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 90)) (begin (set! *srfi-libraries* (cons '(srfi 90) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 93)) (begin (set! *srfi-libraries* (cons '(srfi 93) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 94)) (begin (set! *srfi-libraries* (cons '(srfi 94) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 95)) (begin (set! *srfi-libraries* (cons '(srfi 95) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 96)) (begin (set! *srfi-libraries* (cons '(srfi 96) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 97)) (begin (set! *srfi-libraries* (cons '(srfi 97) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 98)) (begin (set! *srfi-libraries* (cons '(srfi 98) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 99)) (begin (set! *srfi-libraries* (cons '(srfi 99) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 100)) (begin (set! *srfi-libraries* (cons '(srfi 100) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 101)) (begin (set! *srfi-libraries* (cons '(srfi 101) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 105)) (begin (set! *srfi-libraries* (cons '(srfi 105) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 106)) (begin (set! *srfi-libraries* (cons '(srfi 106) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 107)) (begin (set! *srfi-libraries* (cons '(srfi 107) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 108)) (begin (set! *srfi-libraries* (cons '(srfi 108) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 109)) (begin (set! *srfi-libraries* (cons '(srfi 109) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 110)) (begin (set! *srfi-libraries* (cons '(srfi 110) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 111)) (begin (set! *srfi-libraries* (cons '(srfi 111) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 112)) (begin (set! *srfi-libraries* (cons '(srfi 112) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 113)) (begin (set! *srfi-libraries* (cons '(srfi 113) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 115)) (begin (set! *srfi-libraries* (cons '(srfi 115) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 116)) (begin (set! *srfi-libraries* (cons '(srfi 116) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 117)) (begin (set! *srfi-libraries* (cons '(srfi 117) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 118)) (begin (set! *srfi-libraries* (cons '(srfi 118) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 119)) (begin (set! *srfi-libraries* (cons '(srfi 119) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 120)) (begin (set! *srfi-libraries* (cons '(srfi 120) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 122)) (begin (set! *srfi-libraries* (cons '(srfi 122) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 123)) (begin (set! *srfi-libraries* (cons '(srfi 123) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 124)) (begin (set! *srfi-libraries* (cons '(srfi 124) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 125)) (begin (set! *srfi-libraries* (cons '(srfi 125) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 126)) (begin (set! *srfi-libraries* (cons '(srfi 126) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 127)) (begin (set! *srfi-libraries* (cons '(srfi 127) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 128)) (begin (set! *srfi-libraries* (cons '(srfi 128) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 129)) (begin (set! *srfi-libraries* (cons '(srfi 129) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 130)) (begin (set! *srfi-libraries* (cons '(srfi 130) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 131)) (begin (set! *srfi-libraries* (cons '(srfi 131) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 132)) (begin (set! *srfi-libraries* (cons '(srfi 132) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 133)) (begin (set! *srfi-libraries* (cons '(srfi 133) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 134)) (begin (set! *srfi-libraries* (cons '(srfi 134) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 135)) (begin (set! *srfi-libraries* (cons '(srfi 135) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 136)) (begin (set! *srfi-libraries* (cons '(srfi 136) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 137)) (begin (set! *srfi-libraries* (cons '(srfi 137) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 138)) (begin (set! *srfi-libraries* (cons '(srfi 138) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 139)) (begin (set! *srfi-libraries* (cons '(srfi 139) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 140)) (begin (set! *srfi-libraries* (cons '(srfi 140) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 141)) (begin (set! *srfi-libraries* (cons '(srfi 141) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 143)) (begin (set! *srfi-libraries* (cons '(srfi 143) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 144)) (begin (set! *srfi-libraries* (cons '(srfi 144) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 145)) (begin (set! *srfi-libraries* (cons '(srfi 145) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 146)) (begin (set! *srfi-libraries* (cons '(srfi 146) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 147)) (begin (set! *srfi-libraries* (cons '(srfi 147) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 148)) (begin (set! *srfi-libraries* (cons '(srfi 148) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 149)) (begin (set! *srfi-libraries* (cons '(srfi 149) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 150)) (begin (set! *srfi-libraries* (cons '(srfi 150) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 151)) (begin (set! *srfi-libraries* (cons '(srfi 151) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 152)) (begin (set! *srfi-libraries* (cons '(srfi 152) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 154)) (begin (set! *srfi-libraries* (cons '(srfi 154) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 155)) (begin (set! *srfi-libraries* (cons '(srfi 155) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 156)) (begin (set! *srfi-libraries* (cons '(srfi 156) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 157)) (begin (set! *srfi-libraries* (cons '(srfi 157) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 158)) (begin (set! *srfi-libraries* (cons '(srfi 158) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 159)) (begin (set! *srfi-libraries* (cons '(srfi 159) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 160)) (begin (set! *srfi-libraries* (cons '(srfi 160) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 161)) (begin (set! *srfi-libraries* (cons '(srfi 161) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 162)) (begin (set! *srfi-libraries* (cons '(srfi 162) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 163)) (begin (set! *srfi-libraries* (cons '(srfi 163) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 164)) (begin (set! *srfi-libraries* (cons '(srfi 164) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 165)) (begin (set! *srfi-libraries* (cons '(srfi 165) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 166)) (begin (set! *srfi-libraries* (cons '(srfi 166) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 167)) (begin (set! *srfi-libraries* (cons '(srfi 167) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 168)) (begin (set! *srfi-libraries* (cons '(srfi 168) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 169)) (begin (set! *srfi-libraries* (cons '(srfi 169) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 170)) (begin (set! *srfi-libraries* (cons '(srfi 170) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 171)) (begin (set! *srfi-libraries* (cons '(srfi 171) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 172)) (begin (set! *srfi-libraries* (cons '(srfi 172) *srfi-libraries*)))) (else))
   (cond-expand ((library (srfi 173)) (begin (set! *srfi-libraries* (cons '(srfi 173) *srfi-libraries*)))) (else))

   (cond-expand ((library (scheme base)) (begin (set! *r7rs-libraries* (cons '(scheme base) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme case-lambda)) (begin (set! *r7rs-libraries* (cons '(scheme case-lambda) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme char)) (begin (set! *r7rs-libraries* (cons '(scheme char) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme complex)) (begin (set! *r7rs-libraries* (cons '(scheme complex) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme cxr)) (begin (set! *r7rs-libraries* (cons '(scheme cxr) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme eval)) (begin (set! *r7rs-libraries* (cons '(scheme eval) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme file)) (begin (set! *r7rs-libraries* (cons '(scheme file) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme inexact)) (begin (set! *r7rs-libraries* (cons '(scheme inexact) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme lazy)) (begin (set! *r7rs-libraries* (cons '(scheme lazy) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme load)) (begin (set! *r7rs-libraries* (cons '(scheme load) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme process-context)) (begin (set! *r7rs-libraries* (cons '(scheme process-context) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme read)) (begin (set! *r7rs-libraries* (cons '(scheme read) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme repl)) (begin (set! *r7rs-libraries* (cons '(scheme repl) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme time)) (begin (set! *r7rs-libraries* (cons '(scheme time) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme write)) (begin (set! *r7rs-libraries* (cons '(scheme write) *r7rs-libraries*)))) (else))
   (cond-expand ((library (scheme r5rs)) (begin (set! *r7rs-libraries* (cons '(scheme r5rs) *r7rs-libraries*)))) (else))
   (begin
      (define (silver-paren-features-srfi)
        *srfi-libraries*)
      (define (silver-paren-features-r7rs)
        *r7rs-libraries*)
      ))
